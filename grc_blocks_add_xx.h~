#ifndef __H_GRC_BLOCKS_ADD_XX_
#define __H_GRC_BLOCKS_ADD_XX_

#include "grc_block.h"

class blocks_add_xx: public block {
 public:
  static const object *theObject;
  blocks_add_xx();
  virtual ~blocks_add_xx();
  cppPair assembleStmt(const blockInfo bi);

 private:
  string id;
  string _enabled;
  string type;
  string num_inputs;
  string vlen;
  string affinity;
  string minoutbuf;
  string _coordinate;
  string _rotation;
};

#endif // __H_GRC_BLOCKS_ADD_XX_
